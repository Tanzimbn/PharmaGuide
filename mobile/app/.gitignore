node_modules/
.expo/
dist/
# native dirs are generated by `expo prebuild` for the dev build
/ios/
/android/
*.log
.DS_Store
# secrets — never commit a real LLM key (public repo). See .env.example
.env
.env.local
